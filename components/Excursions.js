import React, {useState} from 'react';
import { styles } from '../styles/style';
import { StyleSheet, Text, View, SafeAreaView, Button, Alert, Image, TouchableOpacity, TouchableWithoutFeedback, Platform, ScrollView } from 'react-native';

export default function Excursions({ navigation }) {

    return (
        <SafeAreaView style={styles.safeAreaContainer}>
            <Text>Excursions</Text>
            
        </SafeAreaView>
    );
}